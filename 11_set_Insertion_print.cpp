#include<iostream>
#include<set>
using namespace std;

int main(){

    // set<int> s;
    // set<int, greater<int>> s;
    // s.insert(1);
    // s.insert(2);
    // s.insert(3);
    // s.insert(4);
    // s.erase(3);
    // s.erase(s.begin());

    // for(auto i : s)
    //     cout<< i << " ";
    // cout<<endl;
    // for (auto i = s.begin(); i != s.end(); i++)
    //     cout<< *i << " ";
    // cout<<endl;

    // for (auto i = s.rbegin(); i != s.rend(); i++)
    //     cout<< *i << " ";
    // cout<<endl;
    // cout<< *s.lower_bound(2) <<endl;
    // cout<< *s.lower_bound(4) <<endl;
    // cout<< (s.upper_bound(4) == s.end())<<endl;

    // multiset<int> s;
    // s.insert(1);
    // s.insert(3);
    // s.insert(3);
    // s.insert(3);
    // s.insert(5);
    // // s.erase(3);
    // s.erase(s.find(3));

    // for(auto i : s)
    //     cout<< i <<" ";

    
    
    


    return 0;
}